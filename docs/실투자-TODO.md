# 실투자 전환 TODO

현재 상태: 페이퍼 트레이딩 (가상매매) 완성
목표: KIS API 실전계좌 연동 자동매매

---

## 1. KIS API 실전계좌 설정

- [ ] KIS 개발자센터에서 **실전투자 앱키/시크릿** 별도 발급
  - 현재 키(`KIS_APP_KEY`)가 모의투자용인지 실전용인지 확인 필요
  - 실전: `https://openapi.koreainvestment.com:9443`
  - 모의: `https://openapivts.koreainvestment.com:29443`
- [ ] `.env`에 계좌번호 추가
  ```
  KIS_CANO=종합계좌번호(8자리)
  KIS_ACNT_PRDT_CD=계좌상품코드(01 등)
  KIS_MODE=real  # real | paper
  ```
- [ ] `rest_client.py` BASE_URL을 모드에 따라 분기

---

## 2. KIS 주문 API 구현 (`kis/rest_client.py`)

- [ ] **매수 주문** `place_buy_order(code, qty, price=0)`
  - tr_id: `TTTC0802U` (실전 시장가), `TTTC0801U` (실전 지정가)
  - 모의: `VTTC0802U` / `VTTC0801U`
- [ ] **매도 주문** `place_sell_order(code, qty, price=0)`
  - tr_id: `TTTC0801U` (실전), `VTTC0801U` (모의)
- [ ] **주문 취소** `cancel_order(order_no)`
- [ ] **체결 확인** `get_order_status(order_no)` — 체결/미체결 폴링
- [ ] **잔고 조회** `get_balance()` — 실제 보유 종목/수량/평균단가
- [ ] **예수금 조회** `get_cash_balance()` — 주문 가능 금액

---

## 3. 실투자 엔진 구현 (`core/live_engine.py`)

- [ ] `paper_engine.py` 구조 참고해서 `LiveEngine` 클래스 작성
- [ ] 핵심 차이점:
  - `_do_open()` → KIS 매수 주문 API 호출 → 체결 확인 후 포지션 등록
  - `_do_close()` → KIS 매도 주문 API 호출 → 체결 확인 후 포지션 제거
  - 포지션 상태를 KIS 잔고와 주기적으로 동기화 (서버 재시작 대응)
- [ ] **시장가 vs 지정가** 전략 결정
  - 시장가: 즉시 체결 보장, 슬리피지 발생 가능
  - 지정가: 현재가 ±0.5% 범위로 제출 권장

---

## 4. 리스크 관리 강화

- [ ] **일일 최대 손실 한도** 설정 (예: -3% 도달 시 당일 매수 중단)
- [ ] **종목당 최대 투자 비율** 검증 (현재 15% — 실전에서도 동일 적용)
- [ ] **동시 포지션 수** 재검토 (현재 max 2 — 자본 규모에 맞게 조정)
- [ ] **긴급 전량 청산 버튼** UI 및 API 추가 (현재 페이퍼에만 있음)
- [ ] 장 마감 15분 전(15:05) 미청산 포지션 자동 청산 옵션

---

## 5. 수수료/세금 반영

- [ ] 현재 수수료: `0.1%` (commission_rate) → 실전 증권사 수수료로 조정
  - 키움: 온라인 0.015% / 한국투자: 온라인 0.015% 수준
- [ ] **증권거래세** 매도 시 추가
  - KOSPI: 0.20%, KOSDAQ: 0.20% (2025년 기준)
- [ ] 수익률 계산에 세후 기준 반영

---

## 6. 알림 시스템

- [ ] **텔레그램 봇** 연동 (매수/매도/손절 발생 시 즉시 알림)
  - 또는 카카오 알림톡
- [ ] 알림 내용: 종목명, 진입/청산가, 수익률, 잔고
- [ ] 일일 성과 리포트 자동 발송 (장 마감 후)

---

## 7. 모니터링/안전장치

- [ ] **페이퍼 ↔ 실전 모드 명확히 분리** — 설정 실수로 실주문 발생 방지
  - UI에 "실전투자 모드" 경고 배너 표시
- [ ] KIS API 호출 실패 시 재시도 로직 (네트워크 순단 대응)
- [ ] 주문 후 N초 내 미체결 시 취소 후 재주문 또는 알림
- [ ] DB 장애 시에도 주문 기록 로컬 파일 백업

---

## 8. 백테스트 검증 (실투자 전 필수)

- [ ] 최근 3개월 데이터로 개선된 조건(손절 -2%/-4%, 13:30 컷, 고점필터) 백테스트
- [ ] 승률 60% 이상 / MDD -15% 이하 확인 후 실전 전환
- [ ] 종목별 백테스트 결과 리포트 저장

---

## 전환 순서 (권장)

```
1단계: KIS 실전 앱키 발급 + 계좌번호 설정
2단계: 잔고/예수금 조회 API 구현 및 테스트
3단계: 소액(50만원) 수동 주문 API 테스트
4단계: LiveEngine 구현 + 페이퍼와 병렬 실행 (비교 검증)
5단계: 소액 자동매매 1주일 실전 테스트
6단계: 문제 없으면 자본 규모 확대
```
